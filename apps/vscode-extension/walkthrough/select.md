# Bind a folder to an environment

Each workspace folder syncs with one Keep environment.

1. Open the folder you want to manage.
2. Run **Keep: Select environment**.
3. Pick a project, then an environment.

The Keep status-bar item shows the bound environment and the vault lock
state. You can also browse everything from the **Keep** view in the Activity
Bar.
