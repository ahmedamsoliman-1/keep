# Bind a folder to an environment

Each workspace folder syncs with one Envault environment.

1. Open the folder you want to manage.
2. Run **Envault: Select environment**.
3. Pick a project, then an environment.

The Envault status-bar item shows the bound environment and the vault lock
state. You can also browse everything from the **Envault** view in the Activity
Bar.
